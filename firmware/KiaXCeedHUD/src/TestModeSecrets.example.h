#pragma once
// Copy to TestModeSecrets.h to enable development-only LAN access.
// TestModeSecrets.h is ignored by Git and must never be committed.
#define HUD_TEST_MODE 1
#define HUD_TEST_WIFI_SSID "your-ssid"
#define HUD_TEST_WIFI_PASSWORD "your-password"
#define HUD_TEST_MDNS_NAME "kia-hud"
